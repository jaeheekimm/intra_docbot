[theme]
base = "light"

# 파란 계열 포인트 컬러
primaryColor = "#055aaf"

# 메인 배경 - 거의 흰색에 가까운 차가운 화이트
backgroundColor = "#F5F7FA"

# 사이드바 배경 - 차분한 네이비 계열
secondaryBackgroundColor = "#e4effb"

# 텍스트
textColor = "#0D1B2A"

# 맑은고딕에 가까운 sans-serif 계열
font = "sans serif"